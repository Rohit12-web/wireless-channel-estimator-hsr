# Data

This study uses simulation-generated channel realizations; no external dataset is
required by the main publication pipeline.

The channel generator, train trajectory, pilot observation model, Rician fading,
Doppler evolution, and evaluation sampling are defined directly in:

`src/publication_pipeline_v6_transactions_final.py`

Do not upload temporary training tensors or duplicated generated datasets to the
source repository.
